Homepage-only update to the main handoff (2026-09-24). `site/index.dc.html` is the prototype; it
imports SiteHeader / SiteFooter / support.js / alloy-motion.js from `../redesign-handoff/site/`
(identical files, not duplicated here). Fonts and assets are the same as `/public`.

v2 (2026-09-24): MatchHOA wordmark replaces the text link in the Network leads band; assets in `assets/` and `/public/assets/match-hoa-white.svg` (+ two-tone `match-hoa-wordmark.svg`, unused).
